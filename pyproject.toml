[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "crashdiag"
version = "0.3.2"
description = "Mechanically verified infrastructure-diagnosis RL environment"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "CrashDiag contributors" }]
license = "CC-BY-4.0"
dependencies = []

[project.urls]
Homepage = "https://github.com/Indium-AI-Labs/CrashDiag"
Documentation = "https://github.com/Indium-AI-Labs/CrashDiag/tree/main/docs"
Repository = "https://github.com/Indium-AI-Labs/CrashDiag.git"
Model = "https://huggingface.co/Indium-AI-Labs/CrashDiag-Qwen2.5-3B-GRPO"

[project.optional-dependencies]
artifacts = [
  "huggingface-hub>=1.5,<2",
  "python-dotenv>=1.0,<2",
]
train = [
  "accelerate>=1.10,<2",
  "bitsandbytes>=0.46,<1",
  "datasets>=4,<5",
  "huggingface-hub>=1.5,<2",
  "peft>=0.18,<1",
  "python-dotenv>=1.0,<2",
  "torch>=2.6",
  "transformers>=5.2,<6",
  "trl>=1.8,<2",
]

[project.scripts]
crashdiag-sandbox = "crashdiag.sandbox_server:main"
crashdiag-generate = "training.generate_dataset:main"
crashdiag-generate-grpo-hard = "training.generate_grpo_hard:main"
crashdiag-sft = "training.sft:main"
crashdiag-grpo = "training.grpo:main"
crashdiag-grpo-pipeline = "training.grpo_pipeline:main"
crashdiag-calibrate-grpo = "training.calibrate_grpo:main"
crashdiag-evaluate = "training.evaluate:main"
crashdiag-evaluate-jsonl = "training.evaluate_jsonl:main"
crashdiag-kaggle = "training.kaggle:main"

[tool.setuptools.packages.find]
include = ["crashdiag*", "training*"]
